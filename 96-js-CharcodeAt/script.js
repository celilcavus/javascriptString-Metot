var icerik = "Celil Cavus";
document.write(icerik + "</br>");

var result = icerik.charCodeAt(icerik.length - 1);

   // var result = icerik.charCodeAt(0);
   //    document.write(result);

for (let index = 0; index < icerik.length; index++) {
   var result = icerik.charCodeAt(index);
   document.write(result + "</br>");
   
}